import React from "react";
import ReactDOM from "react-dom/client";
import WealthAuthApp from "./App";
import "../styles/wealth-auth.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <WealthAuthApp />
  </React.StrictMode>
);
